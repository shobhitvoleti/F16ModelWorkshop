### DO NOT EDIT THIS FILE
### This file is auto-generated by the Dyad command-line compiler.
### If you edit this code it is likely to get overwritten.
### Instead, update the Dyad source code and regenerate this file


using DyadInterface
using DyadInterface: ODEAlg, DEVerbosity, OptimizationLevel
using ModelingToolkit: SymbolicT, toggle_namespacing
using DyadInterface: AbstractTransientAnalysisSpec, TransientAnalysisSpec
@kwdef mutable struct TestSampledIOTransientSpec <: AbstractTransientAnalysisSpec
  name::Symbol = :TestSampledIOTransient
  var"alg"::ODEAlg.Type = ODEAlg.Auto()
  var"start"::Float64 = 0
  var"stop"::Float64 = 1.0
  var"abstol"::Float64 = 0.000001
  var"reltol"::Float64 = 0.000001
  var"saveat"::Float64 = 0
  var"dtmax"::Float64 = 0
  var"tstops"::Array{Float64, 1} = []
  var"automatic_discontinuity_detection"::Bool = false
  var"optimize"::OptimizationLevel.Type = OptimizationLevel.Aggressive()
  var"progress"::Bool = true
  var"respecialize"::Bool = false
  var"verbose"::DEVerbosity.Type = DEVerbosity.Standard()
  var"log_file"::String = ""
  # Minimal validation of the vector clock-crossing pair.
  # 
  # A single continuous ramp feeds a 1-channel `MultiSampler`; the sampled signal is
  # held back to continuous by a 1-channel `MultiZeroOrderHold`. A 10 Hz
  # `PeriodicClock` is planted on the clocked junction. A downstream `Integrator`
  # consumes the held signal so the otherwise purely-clocked model carries a real
  # ODE state (an empty `u0` crashes the auto-dt initializer).
  # 
  # Expected: `retcode == Success`; `intg.x` accumulates the zero-order-hold
  # staircase of the ramp sampled every 0.1 s (per-interval increments
  # `[0.0, 0.1, ..., 0.9] * 0.1`, total 0.45 at t = 1 s).
  # 
  # A single channel is sufficient to validate the wrapper mechanics (array
  # comprehension, clock inference, sample-hold physics). The multi-channel vector
  # path is exercised end-to-end by `F16DiscreteClosedLoop`, where the controller's
  # shared block clock unifies all channels.
  var"model"::Union{Nothing, System} = F16ModelWorkshop.VectorBlocks.TestSampledIO(; name=:TestSampledIO)
end

function DyadInterface.run_analysis(spec::TestSampledIOTransientSpec)
  overrides = Dict{SymbolicT, SymbolicT}()
  no_namespace_model = toggle_namespacing(spec.model, false)
  
  base_spec = TransientAnalysisSpec(;
    name=:TransientAnalysis, overrides, alg=spec.alg, start=spec.start, stop=spec.stop, abstol=spec.abstol, reltol=spec.reltol, saveat=spec.saveat, dtmax=spec.dtmax, tstops=spec.tstops, automatic_discontinuity_detection=spec.automatic_discontinuity_detection, optimize=spec.optimize, progress=spec.progress, respecialize=spec.respecialize, verbose=spec.verbose, log_file=spec.log_file, model=spec.model
  )
  run_analysis(base_spec)
end

TestSampledIOTransient(;kwargs...) = run_analysis(TestSampledIOTransientSpec(;kwargs...))
export TestSampledIOTransient, TestSampledIOTransientSpec
