### DO NOT EDIT THIS FILE
### This file is auto-generated by the Dyad command-line compiler.
### If you edit this code it is likely to get overwritten.
### Instead, update the Dyad source code and regenerate this file


import Moshi as __Ext__Moshi

@doc Markdown.doc"""
   DiscreteStateSpace(; name, nx, nu, ny, A, B, C, D, x0, u0, y0)

Discrete-time, clocked linear state-space block.

Realizes the *discrete* difference equation:

```
x[k] = A*x[k-1] + B*(u[k-1] - u0)      (state update)
y[k] = C*x[k]   + D*(u[k]   - u0) + y0  (output)
```

where `A`/`B`/`C`/`D` are the **already-discrete** state-space matrices (e.g. a
ZOH-discretized controller). There is no internal discretization — pass discrete
matrices directly. The realization carries an inherent one-sample input delay in
the state path (`x[k]` depends on `u[k-1]`), which is the canonical form for a
strictly-proper sampled-data controller (`D = 0`).

The block is clocked: its sample rate is inferred from the clock reaching its
connectors (place a `PeriodicClock` upstream of `u`, typically through a
`Sampler`). The state register is built from `nx` scalar
`DiscreteComponents.UnitDelay` blocks, each initialized to `x0[i]`.

A `ClockedZero` anchor (`z0`) is added to every output row. It contributes a
clocked `0.0`, leaving the numeric output unchanged, but keeps each row on the
block clock partition even when a row of `C` (and `D`) is all zeros — which would
otherwise fold to the constant `y0[j]` and orphan the clock partition during
`mtkcompile`. See `SYNCHTOOLKIT_ISSUES.md`, Issue 1.

Connectors (matching `BlockComponents.Continuous.StateSpace`):
- `u :: RealInput[nu]`  (clocked) — input signal.
- `y :: RealOutput[ny]` (clocked) — output signal.

Parameters:
- `nx`        — state dimension.
- `nu`        — number of inputs  (length of `u`).
- `ny`        — number of outputs (length of `y`).
- `A` (nx×nx) — discrete state matrix.
- `B` (nx×nu) — discrete input matrix.
- `C` (ny×nx) — discrete output matrix.
- `D` (ny×nu) — discrete feedthrough matrix.
- `x0` (nx)   — initial state (register seed).
- `u0` (nu)   — input operating point.
- `y0` (ny)   — output operating point.

## Parameters:

| Name         | Description                         | Units  |   Default value |
| ------------ | ----------------------------------- | ------ | --------------- |
| `nx`         | Dimension of state vector                         | --  |   2 |
| `nu`         | Number of inputs                         | --  |   1 |
| `ny`         | Number of outputs                         | --  |   1 |
| `A`         | Discrete state matrix (nx × nx)                         | --  |   fill(0.0, nx, nx) |
| `B`         | Discrete input matrix (nx × nu)                         | --  |   fill(0.0, nx, nu) |
| `C`         | Discrete output matrix (ny × nx)                         | --  |   fill(0.0, ny, nx) |
| `D`         | Discrete feedthrough matrix (ny × nu)                         | --  |   fill(0.0, ny, nu) |
| `x0`         | Initial state (register seed)                         | --  |   zeros(nx) |
| `u0`         | Input operating point                         | --  |   zeros(nu) |
| `y0`         | Output operating point                         | --  |   zeros(ny) |

## Connectors

 * `u` - This connector represents a real signal as an input to a component ([`RealInput`](@ref))
 * `y` - This connector represents a real signal as an output from a component ([`RealOutput`](@ref))

## Variables

| Name         | Description                         | Units  | 
| ------------ | ----------------------------------- | ------ |
| `x`         | Current state (delay outputs collected into a vector)                         | --  |
| `xn`         | Next state fed into the delays                         | --  |
| `du`         | Input deviation from operating point                         | --  |
"""
@component function DiscreteStateSpace(; name = nothing, nx=2, nu=1, ny=1, A=fill(0.0, nx, nx), x0=zeros(nx), B=fill(0.0, nx, nu), u0=zeros(nu), C=fill(0.0, ny, nx), D=fill(0.0, ny, nu), y0=zeros(ny), kwargs...)
  isnothing(name) && throw(ArgumentError("""
    The `name` keyword must be provided. Please consider using the `@named` macro,
    like so:
  
    @named model = DiscreteStateSpace()
  """))

  __overrides = __build_overrides(kwargs)
  __params = Symbolics.SymbolicT[]
  __vars = Symbolics.SymbolicT[]
  __systems = System[]
  __guesses = Dict{Symbolics.SymbolicT, Symbolics.SymbolicT}()
  __initial_conditions = Dict{Symbolics.SymbolicT, Symbolics.SymbolicT}()
  __initialization_eqs = Equation[]
  __eqs = Equation[]
  __bindings = Dict{Symbolics.SymbolicT, Symbolics.SymbolicT}()

  ### Structural Parameters (functions)

  ### Structural Parameters (Final)

  ### Path Parameters (functions)

  ### Path Parameters (non-final)

  ### Final Parameters (declarations)

  ### Deferred assignment (default values that depend on final parameters)

  ### Symbolic Parameters
  __local__u0 = u0
  append!(__params, @parameters (u0[1:nu]::Real), [description = "Input operating point"])
  __initial_conditions[u0] = __local__u0
  __local__y0 = y0
  append!(__params, @parameters (y0[1:ny]::Real), [description = "Output operating point"])
  __initial_conditions[y0] = __local__y0

  ### Final Parameters (assignments)

  ### Final Path Parameters
  append!(__vars, @variables (u(t)[1:nu]::Real), [input = true])
  append!(__vars, @variables (y(t)[1:ny]::Real), [output = true])

  ### Variables (declarations)
  append!(__vars, @variables (x(t)[1:nx]::Real), [description = "Current state (delay outputs collected into a vector)"])
  append!(__vars, @variables (xn(t)[1:nx]::Real), [description = "Next state fed into the delays"])
  append!(__vars, @variables (du(t)[1:nu]::Real), [description = "Input deviation from operating point"])

  ### Variables (assignments)
  __ovr_x = pop!(__overrides, "x", nothing); isnothing(__ovr_x) || push!(__eqs, x ~ __ovr_x)
  __ovr_x__initial = pop!(__overrides, "x__initial", nothing); isnothing(__ovr_x__initial) || (__initial_conditions[x] = __ovr_x__initial)
  __ovr_xn = pop!(__overrides, "xn", nothing); isnothing(__ovr_xn) || push!(__eqs, xn ~ __ovr_xn)
  __ovr_xn__initial = pop!(__overrides, "xn__initial", nothing); isnothing(__ovr_xn__initial) || (__initial_conditions[xn] = __ovr_xn__initial)
  __ovr_du = pop!(__overrides, "du", nothing); isnothing(__ovr_du) || push!(__eqs, du ~ __ovr_du)
  __ovr_du__initial = pop!(__overrides, "du__initial", nothing); isnothing(__ovr_du__initial) || (__initial_conditions[du] = __ovr_du__initial)

  ### Constants
  __constants = Any[]

  ### Components
  # Subcomponent d of type DiscreteComponents.UnitDelay
  d_overrides = __pop_subcomponent_overrides!(__overrides, "d")
  d = System[]
  for i in 1:nx
    push!(d, DiscreteComponents.UnitDelay(initial_condition=ModelingToolkit.default_to_parentscope(x0[i]), name=Symbol("d", "⸺", i); d_overrides...))
  end
  append!(__systems, d)
  # Subcomponent z0 of type F16ModelWorkshop.Controls.ClockedZero
  z0_overrides = __pop_subcomponent_overrides!(__overrides, "z0")
  push!(__systems, @named z0 = F16ModelWorkshop.Controls.ClockedZero(; z0_overrides...))

  ### Check there are no unmatched overrides
  isempty(__overrides) || throw(ArgumentError("overrides: [$(join(keys(__overrides), ", "))] don't match names found in model. These names may exist in the model but could have been conditionally excluded."))

  ### Guesses

  ### Initialization Equations

  ### Assertions
  __assertions = []

  ### Equations
  push!(__eqs, du ~ u - u0)
  push!(__eqs, xn ~ A * x + B * du)
  push!(__eqs, y ~ C * x + D * du + y0 + z0.y * fill(1.0, ny))

  ### Control Structures
  for i in 1:nx
    push!(__eqs, x[i] ~ getproperty(d[i], :y))
    push!(__eqs, getproperty(d[i], :u) ~ xn[i])
  end

  # Return completely constructed System
  return System(__eqs, t, __vars, __params; systems=__systems, initial_conditions=__initial_conditions, guesses=__guesses, name, initialization_eqs=__initialization_eqs, bindings=__bindings, assertions=__assertions)
end
export DiscreteStateSpace
